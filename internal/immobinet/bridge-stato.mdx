---
title: "ImmobiNet Bridge: stato, cosa manca, next step"
sidebarTitle: "Stato del bridge"
description: "Stato del repo immobinet-bridge: cosa è già fatto, cosa è ancora stub, il collo di bottiglia (credenziali cliente) e gli step operativi per portarlo live."
---

Il bridge (`immobinet-bridge`) è il componente che estrae i dati da ImmobiNet via SOAP e li espone come endpoint HTTP puliti, così Conduit (via [Custom Tools](/internal/immobinet/conduit-custom-tools)) e il [Direct Booking](/internal/immobinet/panoramica) possono consumarli. Questa pagina è la fonte di verità sullo stato.

## In una riga

**Fattibilità ALTA, lavoro per lo più GIÀ FATTO.** Bridge SOAP→REST scaffoldato, endpoint SOAP validato live, schema ImmobiNet ↔ Conduit ~1:1. **Collo UNICO = le credenziali del cliente.** Da quando arrivano: **~2 giorni alla live.**

## Repo

- **Percorso locale**: `~/Projects/immobinet-bridge` (Next.js 16 + Turbopack + TypeScript).
- **GitHub**: `ftaiano100-dot/immobinet-bridge` — **privato**, già pushato e allineato (2 commit puliti). Non c'è rischio di perdere il lavoro.

<Note>
La memoria progetto citava il vecchio percorso `~/Desktop/immobinet-bridge` con nota "mai pushato". **Superato**: il codice vive in `~/Projects/immobinet-bridge` (regola: il codice sta in `~/Projects`, non su Desktop/iCloud) ed è già su GitHub privato.
</Note>

### File principali

| File | Ruolo | Stato |
|---|---|---|
| `src/lib/immobinet-soap.ts` | Client SOAP hand-rolled (fetch + fast-xml-parser, no strong-soap) | Envelope + auth **fatti**; parser risposte **stub** |
| `src/lib/mappers.ts` | Funzioni pure: ImmobiNet raw → shape Conduit | Struttura pronta; `mapStatus` provvisorio |
| `src/lib/conduit-types.ts` | Schema canonico Conduit (Contact, Reservation, Listing) | Fatto |
| `src/lib/auth.ts` | Validazione Bearer token (Conduit → bridge) | Fatto, ma **single-tenant**: confronto contro un unico `BRIDGE_API_KEY` globale. Sostituito dall'`X-Student-Token` del gateway |
| `src/app/v1/{reservations,listings,contacts}/route.ts` | Endpoint REST in shape Conduit `{data, next_cursor, has_more}` | Fatto (wiring); dipendono dal parser |
| `docs/ARCHITECTURE.md`, `docs/CONDUIT-SCHEMA.md` | Doc interna repo | Fatto |

## Cosa è già FATTO

- **Endpoint SOAP validato live** (2026-05-11): probe su `https://rentallab.immobinet.it/bundle/webservicemodagenzia/service` → HTTP 401 "Non autorizzato" con **envelope parsato correttamente** = server vivo, formato confermato, auth check attivo.
- **Auth model chiarito**: `username`/`password` in chiaro nel body, nessun WSSE/token/HMAC. Il client costruisce l'envelope a mano coi namespace giusti (`imw` per `<auth>`, `core` per username/password).
- **Bundle Prenotazioni confermato pubblico**: `getPrenotazioni`, `setPrenotazione`, `editPrenotazione`, `delPrenotazione` sono in `webservicemodagenzia` — non serve piano enterprise.
- **Schema mappato**: `Prenotazione` (id, immobile, periodo, cliente, prezzo, note, persone, letti, stato/confermata/eliminata/timestamp) e `cliente` (nome, cognome, email, telefono, lingua, nazione, CF, ...) mappano **quasi 1:1** sul Reservation schema Conduit.

## Cosa è ancora STUB (il collo)

<Warning>
Il **parser delle risposte** è ancora uno stub. In `src/lib/immobinet-soap.ts`:
- `getPrenotazioni(...)` invia la richiesta ma `extractReservations(parsed)` ha un `// TODO: parse the prenotazioni array out of the response envelope`.
- `getImmobili()` ha `// TODO: extract once we see a real response`.

Non si può finire il parser **senza vedere l'XML reale**, e per vederlo servono **credenziali valide del cliente**. Questo è l'unico vero blocker: tutto il resto è meccanico.
</Warning>

## Cosa MANCA per andare live

**Le credenziali ImmobiNet del cliente** (Alberto Milan / Rentallab, ~190 unità a Jesolo — lo studente target ha 100+ immobili). Tre scenari:

| Scenario | Situazione | Lead time |
|---|---|---|
| **A** (più probabile) | Le credenziali del login web ImmobiNet funzionano anche sul webservice | 0 — bastano quelle, 1 chiamata test e completiamo il parser |
| **B** | Servono credenziali webservice dedicate | 1-3 giorni (Alberto scrive a ImmobiNet support) |
| **C** | Il piano del cliente non include il webservice | upgrade ImmobiNet, costo extra da concordare col cliente |

**Primo passo operativo**: recuperare le credenziali e testare lo **Scenario A** (login web del cliente sul webservice).

## Stima tempi residui (dal momento delle credenziali)

| Attività | Effort |
|---|---|
| 1 chiamata test reale + completare `extractReservations()` | 0.5 gg |
| Mapping field-by-field XML → JSON | 0.5 gg |
| Cache layer + retry | 0.5 gg |
| Deploy Vercel + handover Conduit (registrazione custom tools) | 0.5 gg |
| **Totale residuo** | **~2 giorni** |

## Perché ne vale la pena (economics)

Lo studente target ha **100+ immobili** e **non vuole cambiare PMS**. Sbloccare ImmobiNet significa:

- **~€900+/mese** di Conduit per noi (€9/listing × 100+ unità), ricorrenti.
- **Ritenzione del cliente** (evitiamo di perderlo perché "il suo PMS non si integra").

Non è un esperimento marginale: è un caso serio da completare, a fronte di ~2 giorni di lavoro residuo.

## Handover a valle

<Warning>
**Aggiornamento architetturale.** L'handover NON è più "deploy del bridge su Vercel con le env var del cliente". Quel modello è **single-tenant per costruzione** e si moltiplica per ogni PM. Il runtime di destinazione è il **gateway multi-tenant `mcp.mattone.co`**: vedi [Gateway multi-tenant](/internal/conduit-knowhow/gateway-multi-tenant) e [come ImmobiNet ci si innesta](/internal/immobinet/conduit-custom-tools).
</Warning>

1. **Finire il parser qui**, in `immobinet-bridge`, dove c'è già l'envelope corretto: è il posto giusto per iterare sull'XML reale appena arrivano le credenziali.
2. **Portare client + mapper nel gateway** come `lib/immobinet.js` e `lib/immobinet-mcp.js`, **cambiando `getConfig()`**: le credenziali vanno passate come parametro, non lette da `process.env` (è ciò che rende il modulo multi-tenant).
3. **Credenziali nel Vault Supabase** come secret `immobinet_creds_<token>`, non in env var per-cliente. Nuovo studente = riga in `pms_integrations` + secret, **zero deploy**.
4. Registrare `https://mcp.mattone.co/api/mcp/immobinet` + header `X-Student-Token` come **Custom Tools nella dashboard Conduit** (passo manuale: i custom tool non sono creabili via MCP/API).
5. **Self-test runtime** con un `tools/call` read-only prima di dichiarare `connected` — `tools/list` non prova nulla, risponde senza toccare il Vault.
6. Prompting dell'agente + Knowledge per istruirlo a chiamare i tool on-demand.

<Note>
Il repo `immobinet-bridge` resta utile e non va buttato: è dove vive il lavoro sul SOAP (envelope, namespace, parser, mapper, `conduit-types.ts`). Cambia **dove gira**, non **cosa contiene**.
</Note>
