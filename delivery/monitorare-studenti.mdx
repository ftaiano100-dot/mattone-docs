---
title: "Monitorare il lavoro degli studenti"
description: "Cosa guardare, dove, con quali segnali di allarme."
---


Dopo il go-live, il lavoro non è finito: gli agenti vanno **monitorati**. Ecco cosa guardare e dove, per intercettare i problemi prima che lo faccia lo studente.

## ⭐ Prompt: monitora l'avanzamento di uno studente

Se ti serve solo **fotografare a che punto è** uno studente — quali integrazioni sono a posto, cosa manca, qual è la prossima azione — incolla questo prompt in Claude Code. È un check **in sola lettura** (non attiva né provisiona nulla). Diverso dal prompt "Monitora uno studente" più in basso, che guarda invece la **salute runtime** dell'agente (automation rate, escalation, workflow falliti) dopo il go-live.

<Tip>
**▷ Prompt Claude Code — Monitora l'avanzamento di uno studente**  ·  incolla in Claude Code:
</Tip>

```text
Contesto: in questa sessione di Claude Code potrei NON avere il repo/codice
caricato. Per prima cosa apri ENTRAMBI i repo — `~/Projects/api-kross-mattone`
(skill di onboarding, custom tool + catalog.json, provisioning Kross/Conduit) e
`~/Projects/mattone-platform` (connessioni / pms_integrations / Vault / bridge
Notion) — e LEGGI le pagine mattone-docs /delivery/monitorare-studenti e
/onboarding/stato-e-continuazione; per lo stato reale usa gli MCP (Supabase per
pms_integrations e Vault, e — se serve confermare l'agente — Conduit in SOLA
LETTURA: get_automation_rate, list_*, simulate_conversation). Poi procedi:

Monitora l'AVANZAMENTO dell'onboarding dello studente [NOME] e dimmi, per OGNI
integrazione (Anagrafica/Notion, Kross, Conduit, Smart check-in, Stripe), a che
punto è. Per ognuna incrocia tre fonti reali:
1) la vista /connessioni (stato per-connettore, vista studente),
2) la tabella pms_integrations su Supabase (verità macchina + timestamp),
3) il Vault (presenza delle credenziali cifrate — se manca, NON è davvero
   "collegato").
Ricorda che "collegato" != "funziona": dove serve, conferma dal vivo ma in sola
lettura (una prenotazione letta da Kross, le skill core ATTACCATE al Guest Agent
via Conduit, un link Stripe di test).

Restituisci una TABELLA integrazione × [stato attuale | cosa manca | prossima
azione]. Poi in coda: la PROSSIMA azione singola più importante per far avanzare
lo studente, e cosa serve eventualmente dallo studente stesso (così posso
avvisarlo). NON attivare, provisionare o modificare nulla in questo giro: è un
monitoraggio in lettura. Non inventare: se una fonte non è leggibile nella
sessione, dillo.
```

<Note>Perché due repo: **`api-kross-mattone`** tiene le skill di onboarding, i custom tool + `catalog.json` e il provisioning Kross/Conduit (com'è configurato l'agente); **`mattone-platform`** tiene connessioni, `pms_integrations`, Vault e bridge Notion (cosa risulta collegato e con quali credenziali). Servono entrambi per dire davvero a che punto è lo studente.</Note>

## Cosa guardare, dove
| Segnale | Dove si legge | Cosa dice |
|---|---|---|
| Stato connessioni | `pms_integrations` + `/connessioni` | chi è collegato/verificato/in "non ora" |
| Automation rate agente | Conduit `get_automation_rate` | quanto risolve l'AI da sola vs escala |
| Escalation | inbox/escalation Conduit | dove l'AI non ce la fa (temi ricorrenti) |
| Esecuzioni workflow | `list_workflow_execution_events` | quali workflow falliscono e quanto |
| Chiamate voce | `list_calls` (source retell) | se il voice trova le prenotazioni |
| KB knowledge | nodi per-listing | dati critici presenti (WiFi, accesso) o mancanti |

## Segnali di allarme
- **Automation rate che cala** → qualcosa si è rotto (tool/skill/KB) o arrivano casi nuovi.
- **Workflow con FAILED alti** (es. notifica cleaner, WhatsApp welcome): visto su Cosmica con >70% fallimenti → va investigato, non ignorato.
- **Escalation ripetute sullo stesso tema** → manca una skill/KB/listino (candidato ad automazione, vedi [Problemi ricorrenti](/delivery/problemi-ricorrenti-conduit)).
- **KPI knowledge a zero** in piattaforma → possibile lettura sbagliata (path KB), non necessariamente KB vuota.
- **Connessione Notion staccata** → il bridge non ha più la fonte (già successo su Cosmica).

## Ritmo consigliato
- **Check periodico** per studente: connessioni verdi, automation rate, workflow falliti, escalation ricorrenti.
- **Su alert**: se un connettore si stacca, va segnalato subito (in cima), non a fine giro.

<Warning>Molti "non funziona" sono **config**, non codice (tool spenti, skill non attaccate, nomi sbagliati, lingua STT). Guarda prima la config.</Warning>

<Tip>
**▷ Prompt Claude Code — Monitora uno studente**  ·  incolla in Claude Code:
</Tip>

```text
Contesto: in questa sessione di Claude Code NON hai il repo/codice caricato.
Per prima cosa apri il repo `~/Projects/mattone-platform` (connessioni /
pms_integrations) e LEGGI la pagina mattone-docs /delivery/monitorare-studenti;
per lo stato agente/voce usa gli MCP (Conduit: get_automation_rate, list_calls,
execution events; Supabase). Poi procedi:

Fammi un check di monitoraggio dell'agente Conduit dello studente [NOME/
workspace] seguendo mattone-docs /delivery/monitorare-studenti. Leggi: stato
connessioni (pms_integrations), get_automation_rate, escalation recenti
(temi ricorrenti), list_workflow_execution_events (workflow con FAILED alti),
list_calls per la voce, e i KPI knowledge. Dammi un semaforo per area
(verde/giallo/rosso) con la PROVA di ognuno e i 3 problemi più urgenti da
sistemare. Segnala subito se un connettore si è staccato.
```

<CardGroup cols={2}>
  <Card title="Supportare gli studenti" icon="arrow-up-right-from-square" href="/delivery/supportare-studenti">Playbook di supporto</Card>
  <Card title="Problemi ricorrenti Conduit" icon="arrow-up-right-from-square" href="/delivery/problemi-ricorrenti-conduit">Catalogo problema→fix</Card>
</CardGroup>

<Note>Fonti: memoria `reference_conduit_late_checkout_automation` (workflow FAILED), `reference_conduit_fleet_status`, `team-runbook/provisioning-verification-checklist.md`, `project_notion_conduit_kb_bridge`.</Note>
